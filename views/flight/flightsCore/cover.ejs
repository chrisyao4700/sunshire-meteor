<div class="fh5co-hero" style="height: 600px">

    <div class="fh5co-overlay"></div>

    <div class="fh5co-cover" data-stellar-background-ratio="0.5" id="flight-cover">

        <div class="index-content">
            <div class="container">
                <div class="row">
                    <% if(pageMode === 'flight_index'){ %>
                    <% include flightSearch.ejs %>
                    <% } %>
                    <% if(pageMode === 'flight_result'){ %>
                    <% include flightResult.ejs %>
                    <% } %>
                </div>
            </div>
        </div>
    </div>

</div>